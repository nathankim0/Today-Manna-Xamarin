<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:TodaysManna;assembly=TodaysManna" x:Class="TodaysManna.MainPage"
             Title="오늘의 만나">

     <ContentPage.ToolbarItems>
        <ToolbarItem Command="{Binding ReloadCommand}" IconImageSource="Refresh" />
        <ToolbarItem Command="{Binding ShareCommand}" IconImageSource="Share" />
        <ToolbarItem Command="{Binding ToolbarItem_Clicked_Command}" IconImageSource="Info" />
    </ContentPage.ToolbarItems>
    
    <ScrollView>
        <StackLayout Padding="30, 20, 30, 0">
            

            <Label Text="{Binding TodayString}" FontAttributes="Italic" TextDecorations="Underline"/>
           
            <Label Text="{Binding TitleString}" FontAttributes="Bold" FontSize="22"/>

            <Label Text="{Binding AllString}"/>

            <ActivityIndicator Scale="2.5" Color="CadetBlue" IsEnabled="{Binding IsBusy}" IsRunning="{Binding IsBusy}"/>
            
             <!--
            <Button
                Command="{Binding CoppyCommand}"
                IsEnabled="{Binding !IsBusy}"
                FontSize="26"
                VerticalOptions="Start"
                Text="클립보드에 복사"/>
            -->
        </StackLayout>
    </ScrollView>
</ContentPage>