<Application
    x:Class="TodaysManna.App"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <Application.Resources>
        <ResourceDictionary>
            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{AppThemeBinding Dark=#858585, Light=White}" />
                <Setter Property="BarTextColor" Value="{AppThemeBinding Dark=White, Light=Black}" />
            </Style>
            <Style TargetType="Label">
                <Setter Property="FontFamily" Value="batang" />
                <Setter Property="LineHeight" Value="1.5" />
                <Setter Property="TextColor" Value="{AppThemeBinding Dark=White, Light=Black}" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="FontFamily" Value="batang" />
            </Style>
            <Style TargetType="Entry">
                <Setter Property="FontFamily" Value="batang" />
            </Style>
            <Style TargetType="Editor">
                <Setter Property="FontFamily" Value="batang" />
            </Style>
            <Style TargetType="Grid">
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Selected">
                                <VisualState.Setters>
                                    <Setter Property="VisualElement.BackgroundColor" Value="{AppThemeBinding Dark=#2e2e2e, Light=White}" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <Style x:Key="OuterGridStyle" TargetType="Grid">
                <Setter Property="RowDefinitions" Value="40,*" />
            </Style>

            <Style x:Key="HeaderStackLayoutStyle" TargetType="StackLayout">
                <Setter Property="Margin" Value="20,0,20,0" />
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="HorizontalOptions" Value="Fill" />
                <Setter Property="VerticalOptions" Value="Start" />
                <Setter Property="Grid.Row" Value="0" />
            </Style>

            <Style x:Key="HeaderLabelStyle" TargetType="Label">
                <Setter Property="BackgroundColor" Value="Transparent" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="HorizontalOptions" Value="StartAndExpand" />
            </Style>

            <Style x:Key="HeaderGridStyle" TargetType="Grid">
                <Setter Property="HorizontalOptions" Value="End" />
                <Setter Property="RowDefinitions" Value="40" />
            </Style>

            <Style x:Key="HeaderButtonStyle" TargetType="ImageButton">
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="Center" />
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
