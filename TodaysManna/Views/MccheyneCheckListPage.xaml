<ContentPage
    x:Class="TodaysManna.MccheyneCheckListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
    xmlns:local="clr-namespace:TodaysManna"
    ios:Page.UseSafeArea="true"
    BackgroundColor="{AppThemeBinding Dark=White,
                                      Light=White}"
    NavigationPage.BackButtonTitle=""
    NavigationPage.HasBackButton="False"
    NavigationPage.HasNavigationBar="False">

    <Grid x:Name="contentGrid" RowDefinitions="50,*">

        <!--#region HEADER-->
        <StackLayout Style="{x:StaticResource HeaderStackLayoutStyle}">
            <Label Style="{x:StaticResource HeaderLabelStyle}" Text="체크리스트" />
            <Grid Style="{x:StaticResource HeaderGridStyle}">
                <ImageButton Clicked="OnOptionClicked" Style="{x:StaticResource HeaderButtonStyle}">
                    <ImageButton.Source>
                        <FontImageSource
                            FontFamily="materialdesignicons"
                            Glyph="{x:Static local:FontIcons.AppleKeyboardOption}"
                            Size="30"
                            Color="{AppThemeBinding Dark=Black,
                                                    Light=Black}" />
                    </ImageButton.Source>
                </ImageButton>
            </Grid>
        </StackLayout>
        <!--#region HEADER-->

    </Grid>
</ContentPage>